using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Depresion_Intro : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        EventsController.Instance.StartEvent(54);
    }
}
