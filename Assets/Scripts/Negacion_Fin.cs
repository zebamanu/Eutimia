using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Negacion_Fin : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        EventsController.Instance.StartEvent(9);
    }
}
